[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
asyncio_mode = auto
markers =
    asyncio: marks tests as async
    crypto: marks tests as cryptographic
    slow: marks tests as slow (deselect with '-m "not slow"')
